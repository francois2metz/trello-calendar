#!/usr/bin/env node

var argv = require('optimist').default('p', process.env['PORT_WWW'] || 4000).argv;

require('../lib/app').listen(argv.p, function() {
    console.log('listening on http://localhost:'+ argv.p)
});
